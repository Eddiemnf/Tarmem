# Endpoint for the brief assistant and contractor matching.
# It must hold the model API key server-side and accept
#   POST { model, max_tokens, system, messages } -> { text }
# Leave unset and the app says the assistant is not connected; every other
# flow — posting, bidding, milestones, payments — works without it.
VITE_AI_ENDPOINT=
